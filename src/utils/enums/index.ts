export enum FEATURES_ADMIN {
   addMember = "addMember",
   statistical = "statistical",
   users = "users",
   trashes = "trashes",
}
export enum TRASHES_ADMIN {
   users = "users",
}

export enum TAB_PROFILE {
   posts = "posts",
   friends = "friends",
}
